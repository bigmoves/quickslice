FROM ghcr.io/chadmiller/quickslice:latest

# The base image already has:
# - HOST=0.0.0.0
# - PORT=8000
# - EXPOSE 8000

# You can override environment variables here if needed
# ENV PORT=8000
# ENV HOST=0.0.0.0

# Custom lexicons should be mounted to /app/priv/lexicons
# See docker-compose.yml for volume mount example
